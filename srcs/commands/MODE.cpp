#include "MODE.hpp"

MODE::MODE() {}

MODE::~MODE() {}

void	MODE::execute(Server &server, Client *client, std::vector<std::string> &args) const
{
	(void)server;
	(void)client; // TODO
	(void)args;
}
